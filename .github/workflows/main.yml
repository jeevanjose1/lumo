name: Lumo

on: 
  push:
      branches: ['home']
  pull_request:
      branches: ['home']

jobs:
  build-lumo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: action/checkout@v4
      
      - name: Node Setup
        uses: actions/setup-node@v4
        with:
          node-version: 22
      
      - name: Install Deps
        run:  npm i
      
      - name: Build Project
        run: npm run build